# 書きなぐり - PROGRESS.md

## 📋 プロジェクト概要

| 項目 | 内容 |
|------|------|
| アプリ名 | 書きなぐり |
| 一言 | 思いついたことをサッと書くだけの超シンプルメモ |
| リポジトリ | https://github.com/gyaruweijing-tech/kakinaguri |
| 公開URL | https://gyaruweijing-tech.github.io/kakinaguri/ |
| 技術 | HTML / CSS / Vanilla JS（単一ファイル） |
| ホスティング | GitHub Pages |

---

## 🤔 なぜ作ろうと思ったか

- 直近でやりたいこと（1時間後、30分後、5分後とか）が思いついたら、紙に書き出す習慣がある
- でも**紙がない時もある**
- だからスマホ上で同じことをやりたい
- 既存のメモアプリは機能が多すぎて重い。もっと「紙に書きなぐる」感覚のものが欲しかった
- 書いたものは3日で消えていい、そのくらい軽い気持ちで使いたい

---

## ✅ 進捗ログ

### 2026-07-17 - v1.0 初回リリース 🎉

- [x] 紙っぽいUI（罫線付き白紙）
- [x] ＋ボタンで新しいページをめくる（ページフリップアニメーション）
- [x] 左右スワイプでページ切り替え
- [x] localStorage による自動保存
- [x] 3日で自動削除
- [x] PWA対応（ホーム画面に追加可能）
- [x] アプリアイコン作成
- [x] GitHub Pages にデプロイ

### 2026-08-09 - 罫線のしくみを作り直した 🩹

**症状**: 新しいページで1文字打つと、勝手に下に1行増える。

**原因**: 罫線＝行データの `<div>` だったので、「紙が罫線で埋まって見える」ように
最終行に入力があると自動で空行を push していた。しかも push のたびに行リスト全体を
作り直して再フォーカスするので、日本語変換中に打つと入力が飛ぶ状態だった。

**直し方（考え方から変更）**:

- [x] 罫線を「紙に印刷されたもの」= CSS の背景 (`repeating-linear-gradient`) に変更。
      行データが1行でも、紙は最後まで罫線で埋まって見える
- [x] 行が増えるのは **Enter を押したときだけ**。入力しても勝手に増えない
- [x] 入力中は DOM を作り直さない（データ更新のみ）→ 日本語入力が壊れない
- [x] 変換確定の Enter で行が割れないように `isComposing` を見るようにした
- [x] 罫線の余白をタップ → 最後の行にカーソル（空いてなければ1行追加）
- [x] ↑↓ キーで行移動
- [x] 空行には取り消しマーカーの丸を出さない（紙が汚れて見えるので）
- [x] フォーカスを同期実行に（1フレーム遅らせると打鍵が消える／iOSでキーボードが閉じる）

### 2026-08-09 - 「行」をやめて、普通のメモ帳に ✍️

行という単位そのものを捨てて、**紙1枚 = テキスト1つ**にした。長い行は折り返す。

- [x] 1行1入力欄 → **contenteditable 1枚**に変更（`textarea` だと選択範囲だけに
      取り消し線を引けないので contenteditable を採用）
- [x] 長い文章は折り返す。折り返した先も罫線にぴったり乗る
- [x] 取り消し線は **文字を選択 → 出てくるバーから** かけるように（メモ帳と同じ操作）
      - `S` 取り消し線 / `B` 太字 / `解除` 書式を消す
      - 選択の上に出す。紙からはみ出すときは下に出す
- [x] 保存形式を `html` 1本に変更。v1(`text`) / v2(`lines`) からは自動で移行する
      （crossed だった行は `<s>` に変換されるので、書いたものは消えない）
- [x] 保存された HTML はタグ・属性をホワイトリストで洗ってから表示する
- [x] 貼り付け／ドロップはプレーンテキストだけ受け取る（他所の書式を持ち込ませない）
- [x] 文字を選択しているときのドラッグでページをめくらないように

> 💡 文字サイズを変える機能は入れていない。サイズを変えると行の高さが変わって
> 罫線とズレるので、罫線付きの紙とは相性が悪い。

---

## 🚀 今後やるかもしれないこと（やらないかも）

- [ ] オフライン対応（Service Worker）
- [ ] ページの並び替え
- [ ] 手書き入力対応
- [ ] ダークモード
- [ ] 書きなぐったページの共有機能

> 💡 でも、シンプルさが命のアプリだから、機能を増やしすぎないのが大事。

---

## 🏗️ 技術メモ

- **データ保存**: localStorage に JSON で保存。キー `kakinaguri_pages`。
  1ページ = `{ id, html, created }`。旧形式(`text` / `lines`)は読み込み時に自動で `html` に移行
- **エディタ**: `contenteditable` 1枚。書式は `document.execCommand`（`styleWithCSS: false`）で
  `<s>` / `<b>` として入る。表示前に `sanitizeHTML()` でタグと属性を洗う
- **自動削除**: ページ読み込み時に `created` タイムスタンプが3日以上前のものをフィルタリング
- **罫線**: `.lines-container` の `background-image`（`repeating-linear-gradient`）で描画。
  `background-attachment: local` にして中身と一緒にスクロールさせている。
  1行の高さは `--line-height` の1箇所だけで決まる（罫線・行・入力欄が全部これを見る）
- **アニメーション**: CSS transform + transition でページめくり風の動き
- **モバイル対応**: viewport meta + タッチイベントでスワイプ対応
- **PWA**: manifest.json + apple-touch-icon でホーム画面追加対応
